-- =========================================================
-- comments: {{LAYER}} — {{MODULE_SCOPE}}
-- =========================================================
-- Loaded by Bootstrap/Loaders/{{COMMENTS_LOADER}}.sql (post-object creation).

comment on {{OBJECT_TYPE}} {{OBJECT_SIGNATURE}} is
'{{COMMENT_TEXT}}';

-- OBJECT_TYPE examples: table | view | function | procedure
-- Repeat one block per object; keep file grouped by object kind when practical.
