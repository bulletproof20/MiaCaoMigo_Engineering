-- =========================================================
-- MANUAL — {{MODULE_SCOPE}} — {{REFERENCE_TITLE}}
-- =========================================================
-- PURPOSE:   exploratory validation of {{TARGET_API_OR_RULE}}
-- REQUIRES:  init_demo or init_qa + {{FIXTURES}}
-- [optional] Queries/ path for inspection follow-up
-- =========================================================

-- Example invocation (adjust parameters):
select {{API_OR_QUERY}}(
    {{ARGUMENTS}}
);

-- Document expected outcomes in comments; not asserted automatically.
